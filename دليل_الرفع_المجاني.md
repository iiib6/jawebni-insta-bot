# 🚀 دليل رفع n8n مجاناً وبدون حدود (Unlimited Executions)

بما أن اشتراك n8n Cloud المجاني انتهى، أفضل حل هو **استضافة n8n ذاتياً (Self-Hosted)**. بهذا الشكل ستحصل على عدد منفذات (Executions) لا نهائي مجاناً 100%.

---

## ⭐️ الخيار الأول (الأسهل والأسرع): Hugging Face Spaces (مجاني 24/7)

منصة **Hugging Face** توفر سيرفرات Docker مجانية تعمل 24/7 دون أن تنام، وهي ممتازة لتشغيل n8n واستقبال الـ Webhooks.

### خطوات الرفع على Hugging Face:
1. قم بإنشاء حساب مجاني على موقع [HuggingFace.co](https://huggingface.co).
2. من القائمة العلوية اضغط على **Spaces** ثم اختر **Create new Space**.
3. قم بتعبئة البيانات:
   - **Space Name**: اختر أي اسم (مثلاً `my-n8n-bot`).
   - **License**: اختر `MIT` أو أي خيار.
   - **Select the Space SDK**: اختر **Docker** 🐳 ثم اختر **Blank**.
   - **Space Hardware**: اختر **CPU basic - Free**.
4. بعد إنشاء الـ Space، اذهب إلى تبويب **Files** ثم اضغط على **Add file** -> **Create a new file**.
5. سمّ الملف `Dockerfile` وانسخ فيه الكود الموجود في ملف `Dockerfile` المرفق في هذا المجلد:
   ```dockerfile
   FROM n8nio/n8n:latest

   USER root
   RUN mkdir -p /home/node/.n8n && chown -R 1000:1000 /home/node/.n8n

   ENV N8N_PORT=7860
   ENV N8N_PROTOCOL=https
   ENV NODE_ENV=production

   EXPOSE 7860

   USER 1000
   CMD ["n8n", "start"]
   ```
6. اضغط **Commit new file to main**.
7. انتظر دقيقة أو دقيقتين حتى يكتمل البناء (Building)، وستظهر لك شاشة n8n مباشرة!
8. قم بإنشاء حساب المالك (Owner Account) في n8n الخاص بك واستمتع باستخدام غير محدود.

---

## 🛠️ كيف تنقل البوتات بعد فتح n8n الجديد؟

1. افتح واجهة n8n الجديدة.
2. من القائمة الجانبية اختر **Workflows** -> **Import from File**.
3. قم برفع الملفين الموجودين لديك:
   - `بوت انستا لمشروع جاوبني.json`
   - `بوت ماسنجر لمشروع جاوبني - محدث.json`
4. قم بإعادة ربط الـ Credentials (مفاتيح API للانستغرام والماسنجر وفيسبوك).
5. في نود الـ **Webhook**، خذ رابط الـ Webhook الجديد وضعْهُ في إعدادات **Meta Developer Portal / Webhook**.

---

## ⚡ خيارات أخرى متاحة:

- **Render.com**: يمكنك إنشاء Web Service ورفع الـ Dockerfile إليه بنفس الطريقة.
- **Oracle Cloud (Always Free VPS)**: إن كان لديك حساب أو كارت فيزا، يمكنك استئجار سيرفر Ubuntu مجاني مدى الحياة وتثبيت Docker عليه لتشغيل n8n.
